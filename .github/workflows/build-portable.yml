name: Build Desktop Packages

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win
            build-command: npm run electron:build:win
            artifact-path: |
              dist-electron/*.exe
              dist-electron/*.exe.blockmap
              dist-electron/latest.yml
          - os: macos-latest
            platform: mac
            build-command: npm run electron:build:mac
            artifact-path: dist-electron/*.dmg

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Build desktop package
        run: ${{ matrix.build-command }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AnyBot-${{ matrix.platform }}
          path: ${{ matrix.artifact-path }}
          if-no-files-found: error

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: AnyBot ${{ github.ref_name }}
          body: |
            ## AnyBot Packages

            | 平台 | 文件 |
            |------|------|
            | macOS | `AnyBot-*.dmg` |
            | Windows | `AnyBot-Setup-*.exe` |

            ### 本次更新
            - **自动化任务**：支持在本机按分钟、每天、每周或 Cron 触发 Agent 任务。
            - **本地执行过程**：每次自动化执行会新建本地会话，沿用现有 Agent 过程展示、消息落库和变更审核。
            - **结果交付**：自动化结果可保存到本地运行记录，或推送到已启用频道；频道只接收最终结果。
            - **调度策略**：应用重启后不会补跑错过的任务，会直接计算下一次未来执行时间。

            ### 使用说明
            - **macOS**：打开 `.dmg` 后将 `AnyBot.app` 拖到 Applications，再双击启动。
            - **Windows**：运行 `AnyBot-Setup-*.exe` 安装，安装后从开始菜单或桌面快捷方式启动。
            - **Windows 自动更新**：Release 中的 `latest.yml` 和 `.exe.blockmap` 是应用内更新所需文件，请勿删除或重命名。
            - AnyBot 会以内嵌桌面窗口运行，不再额外打开浏览器；Codex/Gemini/Claude/Cursor/Qoder 等 Provider CLI 仍需用户自行安装并登录。
          files: release-assets/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
